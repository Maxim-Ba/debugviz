# WSL / DrvFS (/mnt/c, /mnt/u): pnpm symlinks and hardlinks often fail with "disk I/O error".
# Use hoisted layout + copy instead of linking from the content-addressable store.
node-linker=hoisted
package-import-method=copy
